import { ActiveRouter, MatchResults } from '../../global/interfaces';
/**
  * @name Router
  * @module ionic
  * @description
 */
export declare class Router {
    root: string;
    titleSuffix: string;
    titleSuffixChanged(newValue: string): void;
    activeRouter: ActiveRouter;
    unsubscribe: Function;
    match: MatchResults | null;
    computeMatch(pathname?: string): MatchResults;
    componentWillLoad(): void;
    componentDidUnload(): void;
    render(): JSX.Element;
}
