import { BuildConfig, Manifest } from '../../util/interfaces';
export declare function validateManifestCompatibility(config: BuildConfig, manifest: Manifest): number[];
export declare function calculateRequiredUpgrades(v: Semver): CompilerUpgrade[];
export declare function parseCompilerVersion(version: string): Semver;
export declare function semverLessThan(a: Semver | string, b: Semver | string): boolean;
export interface Semver {
    major: number;
    minor: number;
    patch: number;
    prerelease?: number;
}
export declare const enum CompilerUpgrade {
    JSX_Upgrade_From_0_0_5 = 0,
}
