import { RouterHistory, MatchResults } from '../../global/interfaces';
export declare class TestDemoFour {
    pages: string[];
    match: MatchResults;
    history: RouterHistory;
    handleClick(e: MouseEvent, linkLocation: string): void;
    render(): JSX.Element;
}
